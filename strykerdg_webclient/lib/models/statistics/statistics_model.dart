import 'package:charts_flutter/flutter.dart' as charts;

class StatisticsData {
  final int quantity;
  final String label;
  final charts.Color color;

  StatisticsData({this.quantity, this.label, this.color});
}